<nav class="navbar navbar-expand-lg sticky-top navbar-dark" id="dashboardmenubar">
      <a class="navbar-brand makewhitehover" style="font-size: 15px;" href="/dashboard/<%= guild.id %>">
        <% if (guild.icon) { %>
          <img class="iconpic" src="<%= guild.iconURL %>" width="26" height="26" />
        <% } else { %>
          <span class="noicontext"><%= guild.nameAcronym %></span>
        <% } %>
        <%= guild.name %>
      </a>
    <button class="navbar-toggler" data-toggle="collapse" data-target="#dashboard_collapse_target"><span class="navbar-toggler-icon"></span></button>
     <div class="collapse navbar-collapse justify-content-center" id="dashboard_collapse_target">
       <ul class="nav navbar-nav">
         <li class="nav-item active">
           <a class="nav-link dashnavlink makelightgrey makewhitehover" href="/dashboard/<%= guild.id %>">FEATURES</a>
         </li>
         <li class="nav-item">
           <a class="nav-link dashnavlink makelightgrey makewhitehover" href="/dashboard/<%= guild.id %>/settings">SETTINGS</a>
         </li>
       </ul>
   </div>
 </nav>
