function addGuildData(utils) {

  // let guild = utils.parameter;
  // let embeds = utils.embeds;
  //
  // let MySQL = utils.MySQL;
  //
  // //SQL STATEMENTS
  // let createSettingsTable = "CREATE TABLE IF NOT EXISTS Settings (serverID varchar(18), embeds JSON)";
  // let JSONembeds = {};
  //
  // for (var embed in embeds) {
  //
  //   JSONembeds[embed] =
  //
  // };

}

module.exports = addGuildData;
